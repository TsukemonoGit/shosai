<script lang="ts">
	import { isvalidURL } from '$lib/utils/ogp';

	import type { Snippet } from 'svelte';

	let { url, ok, ng }: { url: string; ok: Snippet; ng: Snippet } = $props();
	let status = $derived(isvalidURL(url));
</script>

{#if status}
	{@render ok()}
{:else}
	{@render ng()}
{/if}
